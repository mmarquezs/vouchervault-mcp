diff --git a/myproject/urls.py b/myproject/urls.py
index 2a03913..3c97f06 100644
--- a/myproject/urls.py
+++ b/myproject/urls.py
@@ -35,4 +35,6 @@ if not settings.OIDC_ENABLED:
 
 # Conditionally include OIDC URLs if OIDC_ENABLED is True
 if settings.OIDC_ENABLED:
-    urlpatterns.append(path('oidc/', include('mozilla_django_oidc.urls')))
\ No newline at end of file
+    urlpatterns.append(path('oidc/', include('mozilla_django_oidc.urls')))
+# extapi: token-authenticated item API overlay (files/docker/docker/vouchervault/)
+urlpatterns.append(path("api/v1/", include("extapi.urls")))
